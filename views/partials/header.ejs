<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notice Board</title>
    <link rel="stylesheet" href="/public/styles.css"> <!-- Include your CSS file -->
</head>
<body>
    <header>
        <h1>College Notice Board</h1>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>

                <% if (!user) { %> <!-- If not logged in -->
                    <li><a href="/login/admin">Admin</a></li>
                    <li><a href="/login/teacher">Teacher</a></li>
                    <li><a href="/login/student">Student</a></li>
                <% } else { %> <!-- If logged in -->
                    <li><a href="/dashboard">Dashboard</a></li>

                    <% if (user.role === "teacher") { %>
                        <li><a href="/myNotices">My Notices</a></li>
                    <% } %>

                    <% if (user.role === "admin") { %>
                        <li><a href="/manageNotice">Manage Notices</a></li>
                        <li><a href="/manageUsers">Manage Users</a></li>
                    <% } %>

                    <li><a href="/logout">Log Out</a></li>
                <% } %>
            </ul>
        </nav>
    </header>
<main>
