<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notice Board</title>
    <link rel="stylesheet" href="/public/styles.css"> <!-- Include your CSS file -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

</head>
<body class="d-flex flex-column min-vh-100">
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container">
                <a class="navbar-brand" href="/">College Notice Board</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        
    
                        <% if (!user) { %> <!-- If not logged in -->
                            <li class="nav-item">
                                <a class="nav-link" href="/login/admin">Admin</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/login/teacher">Teacher</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/login/student">Student</a>
                            </li>
                        <% } else { %> <!-- If logged in -->
                            <li class="nav-item">
                                <a class="nav-link" href="/dashboard">Dashboard</a>
                            </li>
    
                            <% if (user.role === "teacher") { %>
                                <li class="nav-item">
                                    <a class="nav-link" href="/myNotices">My Notices</a>
                                </li>
                            <% } %>
    
                            <% if (user.role === "admin") { %>
                                <li class="nav-item">
                                    <a class="nav-link" href="/manageNotice">Manage Notices</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link" href="/manageUsers">Manage Users</a>
                                </li>
                            <% } %>
    
                            <li class="nav-item">
                                <a class="nav-link btn btn-danger text-white px-3" href="/logout">Log Out</a>
                            </li>
                        <% } %>
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    
<main>
