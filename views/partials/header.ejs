<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notice Board</title>
    <link rel="stylesheet" href="/public/styles.css"> <!-- Your custom CSS if any -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .bg-gradient-dark {
            background: linear-gradient(135deg, #1c1c1c, #343a40);
            background-size: 400% 400%;
            animation: bgFlow 12s ease infinite;
        }

        @keyframes bgFlow {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .animated-header nav {
            animation: fadeInDown 0.8s ease-out;
        }

        @keyframes fadeInDown {
            0% {
                opacity: 0;
                transform: translateY(-20px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .navbar-brand img {
            border-radius: 50%;
            transition: transform 0.3s ease;
        }

        .navbar-brand img:hover {
            transform: scale(1.1) rotate(-5deg);
        }

        .navbar-nav .nav-link {
            transition: all 0.3s;
        }

        .navbar-nav .nav-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body class="d-flex flex-column min-vh-100">

<header class="animated-header">
    <nav class="navbar navbar-expand-lg navbar-dark bg-gradient-dark sticky-top shadow-sm">
        <div class="container">
            <!-- Logo and Title -->
            <a class="navbar-brand d-flex align-items-center gap-2" href="/">
                <img src="https://cdn-icons-png.flaticon.com/512/3135/3135755.png" alt="logo" width="32" height="32">
                <span class="fw-bold fs-5">College Notice Board</span>
            </a>

            <!-- Mobile Toggle Button -->
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <!-- Navigation Links -->
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto align-items-center gap-2">
                    <% if (!user) { %>
                        <li class="nav-item">
                            <a class="nav-link btn btn-outline-light" href="/login/admin">Admin</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link btn btn-outline-light" href="/login/teacher">Teacher</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link btn btn-outline-light" href="/login/student">Student</a>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link text-light" href="/dashboard">Dashboard</a>
                        </li>
                        <% if (user.role === "teacher") { %>
                            <li class="nav-item">
                                <a class="nav-link text-light" href="/myNotices">My Notices</a>
                            </li>
                        <% } %>
                        <% if (user.role === "admin") { %>
                            <li class="nav-item">
                                <a class="nav-link text-light" href="/manageNotice">Manage Notices</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link text-light" href="/manageUsers">Manage Users</a>
                            </li>
                        <% } %>
                        <li class="nav-item">
                            <a class="btn btn-danger px-3" href="/logout">Log Out</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
</header>

<main>
