<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>College Notice Board - Home</title>
  <link rel="stylesheet" href="/public/style.css"/>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">

  <style>
    body {
      background: url('https://images.unsplash.com/photo-1571260899304-425eee4c7efc?auto=format&fit=crop&w=1600&q=80') no-repeat center center fixed;
      background-size: cover;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    .main-container {
      flex: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 1rem;
    }

    .overlay {
      background-color: rgba(255, 255, 255, 0.92);
      backdrop-filter: blur(10px);
      border-radius: 20px;
      padding: 2rem;
      box-shadow: 0 10px 30px rgba(0,0,0,0.15);
      text-align: center;
      max-width: 600px;
      width: 100%;
    }

    .logo {
      width: 100px;
      margin: 0 auto 1rem;
      display: block;
    }

    .typewriter h1 {
  display: inline-block;
  overflow: hidden;
  border-right: 0.15em solid #333;
  white-space: nowrap;
  margin: 0 auto;
  letter-spacing: 1px;
  font-size: 1.6rem;
  font-weight: 700;
  animation: typing 7s steps(60, end) infinite, blink-caret 0.75s step-end infinite;
  max-width: 100%;
  width: 0;
}


    @keyframes typing {
      0% { width: 0 }
      45% { width: 100% }
      55% { width: 100% }
      100% { width: 0 }
    }

    @keyframes blink-caret {
      from, to { border-color: transparent }
      50% { border-color: #333; }
    }

    .btn-lg {
      font-size: 1rem;
      padding: 0.7rem 1rem;
      position: relative;
      overflow: hidden;
      transition: 0.4s ease;
      min-width: 200px;
    }

    .btn-lg::before {
      content: "";
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: rgba(255, 255, 255, 0.2);
      transition: 0.5s;
      transform: skewX(-20deg);
    }

    .btn-lg:hover::before {
      left: 100%;
    }

    .btn-glow:hover {
      box-shadow: 0 0 15px 3px rgba(0, 123, 255, 0.6);
    }

    .footer {
      background-color: rgba(0, 0, 0, 0.75);
      color: white;
      text-align: center;
      padding: 1rem;
      font-size: 0.9rem;
    }

    @media (max-width: 576px) {
      .typewriter h1 {
        font-size: 1.3rem;
      }

      .overlay {
        padding: 1.5rem;
      }

      .btn-lg {
        font-size: 0.95rem;
        min-width: 100%;
      }
    }
  </style>
</head>
<body>

  <div class="main-container">
    <div class="overlay">
      <!-- Moving GIF logo -->
      <img src="https://media4.giphy.com/media/IhIDRwEVka6plLPC9z/giphy.gif" alt="College Logo" class="logo"/>

      <!-- Typewriter heading -->
      <div class="typewriter mb-3">
        <h1><i class="bi bi-mortarboard-fill text-primary me-2"></i>Welcome to the College Notice Board</h1>
      </div>

      <p class="lead mb-4">Select your role to continue:</p>

      <div class="d-flex flex-column flex-sm-row flex-wrap justify-content-center gap-3 mb-4">
        <a href="/login/admin" class="btn btn-primary btn-lg btn-glow">
          <i class="bi bi-person-gear me-2"></i>Login as Admin
        </a>
        <a href="/login/teacher" class="btn btn-success btn-lg btn-glow">
          <i class="bi bi-person-video3 me-2"></i>Login as Teacher
        </a>
        <a href="/login/student" class="btn btn-info btn-lg btn-glow">
          <i class="bi bi-person-circle me-2"></i>Login as Student
        </a>
      </div>

      <div>
        <a href="/signup" class="btn btn-warning btn-lg btn-glow">
          <i class="bi bi-person-plus-fill me-2"></i>Sign-Up a New Admin
        </a>
      </div>
    </div>
  </div>

  <div class="footer">
    © 2025 College Notice Board. All rights reserved.
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
